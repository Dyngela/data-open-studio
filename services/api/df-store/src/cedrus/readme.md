# Parquet Mod

This module aim at providing feature like parquet to store on disk data in a columnar format, which is more efficient for analytical queries. 
It also support compression and encoding to further reduce the storage space and improve the performance.
